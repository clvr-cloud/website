apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/clvr-cloud/website:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/clvr-cloud/website:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: website
        chartPath: charts/website
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/clvr-cloud/website'
          image.tag: '{{.DIGEST_HEX}}'
